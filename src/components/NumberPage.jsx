/*import React from 'react'
import { useParams } from 'react-router-dom'
const NumberPage = () => {
    
    const {number} = useParams()
    return (
        <div>
            {isNumber ? (
                <h1 style={styles}>The number is: {word}</h1>
            ) : (
                <h1 style={styles}>The word is: {word}</h1>
            )}
            
        </div>
    )
    }

export default NumberPage*/